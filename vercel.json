{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "npm install",
        "startCommand": "node src/index.js"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)"
    }
  ]
}
